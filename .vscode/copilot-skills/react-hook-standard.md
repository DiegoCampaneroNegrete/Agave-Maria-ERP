# Skill: React Hook Standard

Use this skill when creating reusable React hooks.

Rules:
- Use TypeScript
- Use Jotai for global state
- Avoid duplicated logic
- Return typed values
- Keep business logic outside UI
- Use descriptive names
- Follow existing project conventions

Example:
Create a hook:
useCartTotals()