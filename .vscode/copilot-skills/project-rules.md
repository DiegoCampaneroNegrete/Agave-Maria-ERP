Always:
- Use TypeScript strict mode
- No duplicated code
- Prefer reusable hooks
- Keep UI clean
- Separate business logic
- Validate all inputs
- Use existing atoms before creating new ones
- Use and create types if is necesary